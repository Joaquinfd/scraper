# --- Núcleo (recomendado, vía API VTEX, sin navegador) ---
requests>=2.31
tenacity>=8.2

# --- Base de datos ---
sqlmodel>=0.0.38
psycopg>=3.3.4
python-dotenv>=1.0.0

# --- Análisis / export opcional ---
pandas>=2.0          # export a Parquet/Excel y análisis posterior
pyarrow>=14.0        # backend parquet para pandas

# --- Alternativas de scraping "pesado" (HTML/JS) ---
# scrapy>=2.11        # framework completo de crawling (ver scrapy_alt/)
# playwright>=1.40    # navegador headless para páginas que exijan JS
#   tras instalar playwright:  playwright install chromium
# beautifulsoup4>=4.12  # parseo HTML cuando no haya API
# lxml>=5.0
